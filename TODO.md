# BibleClip v1.1.6 Feature Specification: Workspace View Mode Architecture

본 문서는 사용자가 환경설정(Settings)에서 성경 본문 표출 방식을 '절별 대조' 또는 '병렬 독서'로 커스텀 선택할 수 있는 보기 모드 인프라와 배후 동기화 엔진 구현 사양을 정의합니다.

---

## ⚙️ 1. Settings Engine Expansion (환경설정 옵션 신설)
- **구현 사양:**
  - 환경설정 UI 단에 `[본문 보기 모드 (View Mode)]` 섹션을 추가할 것.
  - 라디오 버튼 또는 드롭다운을 통해 다음 두 가지 모드를 선택할 수 있도록 데이터 구조(`DEFAULT_SETTINGS`)를 확장하고 영속성 저장(`settings.json`) 체계에 동기화할 것.
    1. **절별 대조 모드 (Verse-by-Verse Mode):** 단일 카드 내에 선택된 역본들이 수직(직렬)으로 교차 표출되는 기존 기본 방식.
    2. **병렬 독서 모드 (Side-by-Side Parallel Mode):** 선택된 역본들이 독립된 카드로 분할되어 좌우(병렬)로 배치되는 영상 기반 신규 방식.

## 🔗 2. Parallel View Sync Engine (병렬 모드 가동 시 자동 링킹)
- **구현 사양:**
  - `View Mode`가 `Parallel`로 활성화된 상태에서 상단 칩 역본이 다중 선택되면, 프론트엔드(`app.js`)는 선택된 역본 개수만큼 본문 카드 뷰포트를 가로 분할(Flex/Grid 레이아웃 바인딩)하여 레이아웃을 전개할 것.
  - **이벤트 락 파이프라인 (Event Lock):** 병렬 정렬된 카드들의 스크롤바 이동(`scroll`) 및 장/절 내비게이션 명령이 트리거될 때, 현재 조작 중인 활성 카드의 픽셀 값 또는 절 ID를 타깃 카드들이 실시간으로 1:1 베지에 트랜지션을 타며 추종하도록 양방향 동기화 스크립트를 빌드할 것. (이때 무한 루프 스택 오버플로우 방지를 위한 센티넬 플래그 제어 필수)