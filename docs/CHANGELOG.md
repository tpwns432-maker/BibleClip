# 변경 이력 (CHANGELOG)

BibleClip의 버전별 변경 내용입니다. 최신 버전이 위에 옵니다.

---

## v1.1.5 — 다중 창 확장 · 발표(F11) 개선 · 장바구니 DnD 재구축 (TODO Part 3)
- **FEAT-07 설교 장바구니 팝아웃 창** — 장바구니를 독립 창(`web/cart.html`)으로 분리. 메인과 **실시간
  양방향 동기화**(`set_cart`가 모든 창에 `onCartChanged`/`renderCartItems` 브로드캐스트), 창에서 성구
  클릭 시 메인 뷰어가 해당 장/절로 점프(`cart_goto`→`onReference` 경로, F11 전체화면 포함). 자식 창은
  `_child_windows` 추적이라 메인 종료 시 함께 닫힘(좀비 방지). 브리지 `open_cart_window`/`get_cart`/
  `set_cart`/`cart_goto`.
- **F11 "틀고정" 위치 헤더** — 전체화면 카드 상단에 현재 위치(`{book_full} {chap}장`)를 flex `order:-1`
  고정 헤더로 표시(본문만 스크롤). 모든 장 네비게이션이 `loadBibleCard`를 거쳐 동기화. 본문과 동일
  읽기 글꼴 사용.
- **장바구니 DnD 전면 재구축** — HTML5 native 드래그가 WebView2에서 무력화(드래그 중 `insertBefore`로
  DOM 이동 시 드래그 중단)되던 것을, 역본 칩과 동일한 **transform 프리뷰 + drop 커밋** 방식으로 교체.
  드래그 중 다른 칩이 `.18s` 슬라이드로 비켜주는 애니메이션. 메인·팝아웃 창 모두 적용.
- **사전 표시 언어 → 표면별 선택**(TODO Part3-1) — 모호하던 toolbar 한/영 토글 + 설정 ⚙ '사전 언어'
  제거. 기본 언어는 **프로그램 언어(ui_lang) 추종**, 사전 카드 헤더 `한/영` pill(전역 공유) + 사전
  팝업 창 드롭다운(창마다 독립, ko·en 임베드 후 순수 JS show/hide)으로 전환.
- **묵상 노트 아이콘 위치 이동**(TODO Part3-2) — 📄 배지를 절 맨 앞 → 본문 문장 끝 뒤로. F11 발표
  모드에서는 `display:none`으로 숨겨 순수 성경 텍스트만 표출.
- **노트 export 구분선**(TODO Part3-5) — 텍스트 파일 내보내기 시 구절 사이에 구분선(50칸) 주입(복사는
  기존대로).
- **창이 화면 밖에 떠 안 보이던 문제 수정** — 최소화 상태로 닫으면 Windows가 `(-32000,-32000)`을
  저장 → 다음 실행 시 화면 밖에 창 생성. `_onscreen()` 가드로 복원·저장 모두에서 off-screen/최소화
  좌표를 거른다.

---

## v1.1.4 — 안정화: 설정/장바구니 영속성 + 좀비 자식 창 수정
- **설정값이 재시작 시 되돌아가던 문제 수정** — ① `load_settings`의 글꼴 크기 클램프가 `min(30)`로
  남아 있어, `set_font_size`(8–400, 방송 송출용으로 캡 해제)로 30 초과 크기를 저장해도 다음 실행에
  30으로 되돌려지던 버그를 `min(400)`로 정렬. ② `reading_font`(읽기 글꼴)·`ui_lang`(화면 언어)·
  `auto_copy_top_result`(검색 자동복사)가 `DEFAULT_SETTINGS`에 없어 — `load_settings`는 기본값에
  존재하는 키만 받아들이므로 — 저장돼도 로드 시 드롭되어 매번 기본값으로 복귀하던 문제를 키 추가로
  해결. ③ 부팅 시 `ui_lang`을 프론트(localStorage, 랜덤 포트로 휘발)→백엔드로 거꾸로 밀어 올바른
  백엔드 값을 덮어쓰던 동기화를 **백엔드 권위**(`init.ui_lang`→`I18N.setLang`)로 뒤집음.
- **설교 장바구니 영속성(FEAT-08)** — 장바구니를 백엔드 파일(`userdata/sermon_cart.json`)에
  저장하도록 전환. 기존엔 `localStorage`에만 저장했는데, pywebview가 매 실행 시 127.0.0.1의
  **랜덤 포트**로 페이지를 서빙해 origin(= localStorage 격리 키)이 매번 바뀌면서 이전 장바구니가
  고아가 되어 재시작 시 비워지던 문제를 해결. `get_initial.cart`로 부팅 시 복구하고, 추가·삭제·
  재정렬마다 `set_cart`로 write-through. (브리지: `get_cart`/`set_cart`, `bibleclip/cart.py`)
- **좀비 자식 창 수정(BUG-SYS)** — 메인 창을 닫아도 우클릭으로 열어둔 '원전 분해' 사전 팝업 창이
  바탕화면에 남던 문제 수정. 생성한 자식 창을 추적(`_child_windows`)했다가 메인 창 `closing` 시
  모두 `destroy()` → pywebview 이벤트 루프(모든 창이 닫혀야 종료)가 깔끔히 종료. 자식 창이
  사용자에 의해 먼저 닫히면 `closed` 이벤트로 추적 목록에서 자동 해제.

---

## v1.1.3 — KJV(흠정역) 성경 기본 동봉
- **KJV+ 성경 동봉** — KJV(1769) + Strong's numbers(`bible_versions/KJV+.SQLite3`, 퍼블릭 도메인)를
  기본 배포에 포함. 이 파일이 `.gitignore`(저작권 가드)로 추적되지 않아 CI checkout에 아예 없었고,
  로컬 빌드(build_web.ps1)에만 동봉돼 그동안 릴리즈에선 빠져 있었음. `.gitignore` 예외로 추적하고,
  CI(build.yml) Windows/macOS Assemble 단계에 KJV+ 복사를 추가(로컬↔CI 빌드 드리프트 해소).
- **exe.config CI 동봉** — v1.1.2에서 로컬 빌드에만 넣었던 `BibleClipWeb.exe.config`
  (loadFromRemoteSources MOTW 백스톱)을 CI Windows Assemble에도 추가.

---

## v1.1.2 — 다운로드 차단(MOTW)으로 실행 안 되던 문제 수정 ★ (일부 PC "런타임 에러"의 진범)
- **다운로드 zip의 Mark-of-the-Web → .NET 어셈블리 로드 거부** 수정. 인터넷에서 받은 zip을 풀면
  내부 모든 파일에 "인터넷에서 옴(Zone.Identifier=3)" 표식이 붙는데, .NET Framework가 표식 달린
  번들 `Python.Runtime.dll`(pywebview→pythonnet CLR 브리지) 로드를 기본 거부 → 시작 실패
  (`Failed to resolve Python.Runtime.Loader.Initialize`). 예외가 winforms/pythonnet 텍스트라
  그동안 ".NET 누락"으로 오인됐다(실제론 .NET·WebView2 정상). **로컬 복사본은 표식이 없어 멀쩡,
  다운로드본만 실패**하던 현장 증상과 정확히 일치.
- **수정**: 시작 시 번들 DLL의 MOTW 표식을 자동 제거(`webui/app.py _strip_motw`, clr import 전 1회).
  백스톱으로 `BibleClipWeb.exe.config`에 `loadFromRemoteSources` 동봉(읽기전용 설치 위치 대비).
  → 이제 사용자가 우클릭 '차단 해제'를 하지 않아도 다운로드본이 바로 실행된다.

---

## v1.1.1 — 실행 실패(빌드 구성) 수정 + 시작 오류 진단·안내 개선
- **실행 실패 수정 / 빌드 재현성** — 일부 PC에서 v1.1.0 릴리즈가 실행되지 않던 문제. 원인은
  CI가 검증되지 않은 조합(Python 3.12)으로 빌드했기 때문(동일 노트북에서 로컬 Python 3.13
  빌드는 정상). CI Windows 빌드를 Python 3.13으로 맞추고 `requirements.txt`를 정확 버전으로
  핀(pywebview 6.2.1 / pythonnet 3.1.0 / clr_loader 0.3.1 / PyInstaller 6.20.0)해 빌드
  재현성을 확보했다. (pythonnet/clr_loader는 Windows 전용 마커로 핀.)
- **시작 실패 안내·로깅 개선** — 시작에 실패하면 실제 .NET / WebView2 / 보안 SW 설치 상태를
  레지스트리로 탐지해 **원인별 안내**(.NET 설치 / WebView2 설치 / 보안 차단 해제)로 분기한다
  (이전엔 무조건 ".NET" 안내 → 오진). 실제 예외 전문 + 환경 프로브를 `userdata/startup_error.log`
  에 기록. 설치 파일 자동 다운로드 대신 **안내 페이지**로 연결(반복 실행 시 설치본이 계속
  떨어지던 불편 제거).

---

## v1.1.0 — 원어 연구 강화(KJV 스트롱) · 다국어 대조/병렬 복사 · 매직 포맷터 · 노트 레일 · 장바구니 DnD

### 새 기능
- **KJV 원어(스트롱) 분석 + 원전 분해 소스 선택** — 영어 KJV 성경의 `<S>NNNN</S>` 스트롱 태그를 파싱해
  한국어 개역한글S와 동일한 UX로 원어를 분석합니다(구약→히브리어 H, 신약→헬라어 G, 책번호 470 기준).
  '원전 분해' 카드 헤더에서 분석 소스 역본(개역한글S / KJV)을 직접 고를 수 있고, 기본값은 UI 언어를
  따릅니다(영어→KJV, 한국어→개역한글S). KJV+(퍼블릭 도메인)는 기본 배포에 동봉됩니다.
- **다국어 대조 보기(FEAT-04)** — 본문 카드의 `[대조]` 토글로 기준 역본과 비교 역본을 한 화면에
  절별로 위아래 나란히 표시합니다. 전역 역본 선택과 독립된, 카드별 역본 쌍입니다.
- **병렬 복사 부스터(FEAT-05)** — 복사 서식 템플릿에 둘째 역본 태그(`{content2}`/`{version2}`)를 넣으면,
  두 역본이 한 번의 복사로 한 블록(한/영 한 세트)으로 묶여 나갑니다.
- **클립보드 매직 포맷터(FEAT-02)** — 출력 설정에서 복사 서식을 직접 정의합니다.
  `{book_full}`·`{book_short}`·`{chap}`·`{verse}`·`{content}`·`{version}` 태그를 칩 버튼으로 커서 위치에 삽입.
- **설교 장바구니 순서 재배치(FEAT-01)** — 담은 성구를 드래그로 순서 변경(드래그 중 다른 항목이 실시간으로
  비켜주는 FLIP 애니메이션), 재배치한 순서대로 추출됩니다.
- **묵상 노트 모아보기 레일(FEAT-03)** — 좌측 네비의 노트 아이콘으로 여는 슬라이딩 패널에서 작성한 모든
  묵상 노트를 성경 순서로 모아 봅니다. 행 클릭으로 본문 이동, 선택/전체를 클립보드 복사 또는 텍스트 파일로 저장.
- **명칭 변경** — '원어 분석' 카드가 '원전 분해'로 바뀌었습니다.

### 버그 수정
- **BUG-01** — 클립보드/F2로 본문을 특정 절로 이동할 때 원어(원전 분해) 카드가 1절에 고정되던 문제 →
  본문이 점프한 절을 그대로 추종합니다.
- **BUG-i18n** — 영어로 전환해도 사전 카드의 기본 안내 문구가 한국어로 남던 문제 → 즉시 재번역됩니다.
- **FIX-01** — F2 빠른 검색으로 잠금 카드로 이동할 때 다른 카드의 스크롤과 엉키던 문제 →
  해당 카드 안에서만 스크롤이 정확히 정렬됩니다(워크스페이스 스크롤 간섭 차단).

---

## v1.0.9 — .NET 런타임 누락 대응(네이티브 안내 + 설치 직링크)
- **`Failed to resolve Python.Runtime.Loader.Initialize` 핫픽스** — .NET Framework 4.7.2 미만/미설치 PC에서
  pywebview(winforms) → pythonnet → .NET CLR 초기화가 실패하면 PyInstaller의 알 수 없는 크래시 창이 뜨던 문제.
  시작 경로를 감싸 이 실패를 감지하면, **.NET·WebView2·로컬 서버가 전혀 필요 없는 순수 Win32 메시지박스**
  (ctypes)로 ".NET Framework가 필요합니다" 안내를 띄우고, [확인] 시 **.NET Framework 4.8 오프라인 설치본**
  (MS 공식 고정 링크)을 브라우저가 바로 내려받게 합니다. 정상 기동에는 영향이 없고, 그 외 오류는 그대로 전파됩니다.

---

## v1.0.8 — 연결 차단 환경 대응(친화적 안내 화면)
- **`ERR_CONNECTION_REFUSED` 핫픽스** — 일부 보안 프로그램·방화벽이 내부 로컬 통신(127.0.0.1 루프백)을
  차단하면 본문 화면이 안 뜨고 브라우저 표준 오류가 노출되던 문제. 부팅 워치독이 프론트의 브리지 도달
  실패를 감지하면, 삭막한 크롬 오류 대신 **다국어 안내 화면**(관리자 권한 실행 · 백신 예외 등록 · 재부팅)을
  표시합니다. 안내는 `NavigateToString`(로컬 서버 미경유)으로 띄워, 루프백이 차단된 바로 그 상황에서도 보입니다.
- **참고(구조 점검)** — 포트 충돌·호스트 바인딩 의심을 점검한 결과, pywebview가 이미 **빈 포트 자동 할당 +
  서버/클라이언트 모두 `127.0.0.1`** 로 동작하고 있어(고정 포트·localhost/IPv6 불일치 없음), 추가 포트/호스트
  코드 없이 외부 차단에 대한 사용자 안내(위)로 대응합니다.

---

## v1.0.7 — 카드 안정성 · 본문↔원어 정렬 · 장바구니 일괄 추출 · 사용자 약칭 UI
- **카드 데이터 보존 / 격리** — 새 카드를 추가·삭제해도 기존 카드의 내용·스크롤이 유지됩니다(예전엔
  카드 하나 추가에 전체를 다시 그려 다른 카드가 비워짐). 추가/삭제가 해당 카드만 증분 mount/unmount 하도록
  변경. 또한 같은 책·장을 띄운 본문 카드들이 자석처럼 함께 움직이던 동기화를 제거해 카드마다 독립 동작합니다.
- **본문↔원어 스크롤 정렬** — 글꼴 종류·크기를 바꿔도 본문 카드와 원어 카드가 같은 절에 정확히 맞춰집니다.
  절-DOM 앵커를 같은 화면 높이(fraction)로 정렬해 첫 절이 잘리던 문제를 없앴고, 글꼴 변경 시 글리프 로드를
  기다린 뒤 읽던 위치(절+위치)를 그대로 복원합니다.
- **설교 장바구니 일괄 추출** — 담아둔 성구를 `전체 추출` 또는 항목별 체크박스로 고른 `선택 추출`로 현재
  출력 형식에 맞춰 한 번에 클립보드로 내보냅니다.
- **사용자 약칭 관리 UI** — 설정 ⚙ → 참조 입력 → 약칭 관리에서 나만의 책이름 약칭을 등록/삭제합니다
  (`bible_versions/aliases_override.json` 직접 편집). 한국어 약칭과 앞숫자 약칭(`1요` → 요한1서, `1Jn` 규격)을
  참조 인식에 반영하도록 파서를 확장했고, 숫자는 책 이름 맨 앞에만 허용합니다(`요1`·`벧1`은 장번호 충돌로 거부).
- **기술 부채 정리** — 카드 복사 토스트의 책이름이 정식/약칭 설정을 따르도록 일원화, 출력 책이름 옵션
  2지선다 정리, 백엔드 로케일 로더 단일화, `Engine._canon` 메모이즈, 글꼴 비례 스텝 보정, i18n 라이브 전환
  단일 진입점(`retranslateViewport`). 출력 설정 미리보기가 역본 추가/삭제에 즉시 반영되도록 수정.

---

## v1.0.6 — 글로벌 다국어(i18n) · 역본별 책이름 · 설교 장바구니 · 전체화면 프레젠테이션
- **다국어 UI(한국어/English)** — 설정 ⚙ → 언어에서 화면 표시 언어를 전환합니다(재시작 없이 즉시).
  UI 문자열을 `web/locales/*.json` 으로 분리한 i18n 인프라 위에 올려, 새 언어는 JSON 파일 하나로 확장됩니다.
- **역본별 책이름** — 각 성경이 자기 책이름으로 표시·복사됩니다(한국어 역본 '룻기', 영어 역본 'Ruth',
  미얀마어 'ရုသ'). 여러 역본을 함께 복사해도 각자 표기를 따릅니다. 책이름 설정은 정식/약칭 2지선다로 정리.
- **다국어 참조 자동 인식** — 클립보드의 영어 약칭(예: `1Ths`)도 인식합니다. 넣어둔 역본들의 책이름을
  자동으로 스캔해 별칭맵을 만들고(하드코딩 0), `bible_versions/aliases_override.json` 으로 수동 보정도 가능합니다.
- **설교 장바구니(큐시트)** — 검색 결과·활동 로그의 `＋` 또는 본문 구절 우클릭으로 성구를 담아두고, 클릭
  한 번에 재복사하거나 본문으로 이동합니다. 담긴 목록은 앱을 닫아도 유지됩니다.
- **전체화면 프레젠테이션(F11/F2)** — `F11` 로 선택한 성구 카드를 전체화면으로(예배·방송 송출), `F2` 퀵
  검색으로 다음 구절을 즉시 전환, `+/−` 로 글자 크기를 조절합니다.
- **글꼴·크기 자유화** — 글자 크기 제한을 풀어 대형 스크린·방송용으로 키울 수 있고, `fonts` 폴더에
  `.ttf/.otf` 글꼴을 넣으면 성경 본문 글꼴을 바꿀 수 있습니다.

---

## v1.0.5 — 똑똑한 다중 키워드 검색 · 하이라이트 · 자동 복사
- **다중 키워드 검색(AND/OR)** — 검색창에 여러 단어를 띄어 입력하면 조사를 자동으로 떼고(태초에 하나님이 →
  태초·하나님) 모든 단어(AND)/하나라도(OR) 포함하는 구절을 찾습니다. 검색창의 `[AND|OR]` 토글로 선택.
- **검색어 하이라이트** — 결과 본문에서 검색한 단어가 색으로 강조됩니다(어간 부분일치 — '창조'가
  '창조하시니라'에서도 강조).
- **관련도 정렬** — 입력 단어가 더 많이·가까이 모이고 짧고 명확한 구절이 상단에 옵니다.
- **최고 결과 자동 복사(선택)** — 설정에서 켜면 검색 1위 구절을 자동으로 클립보드에 복사하고 활동 로그에
  기록합니다(로그 클릭 시 재복사).
- **순수 파이썬 검색 엔진** — 외부 형태소 분석기(Kiwi)나 외장 사전 없이 개역한글 본문을 메모리 역색인으로
  빌드해 빠르고 안정적입니다(배포본 네이티브 크래시 위험 0). 한 단어/구절/원어코드 검색은 그대로 동작.

---

## v1.0.4 — 스크롤 동기화 개선 · 복사 활동로그 통합 · 코드 모듈화
- **스크롤 동기화 개선** — 본문을 스크롤하면 화면 중심(시선) 구절을 기준으로 원어·분할 비교 카드가 더
  안정적으로 따라옵니다. 다음 절이 화면 아래에 살짝 보일 때 카드가 튕기며 강제 전환되던 현상을 줄였습니다.
- **복사 활동 로그 통합** — 구절 클릭·블록 드래그·검색 결과 복사 등 앱 안에서 만든 복사도 좌측 활동
  로그(클립보드 드로어)에 함께 기록됩니다. (기존엔 외부 클립보드 인식만 기록)
- **드래그 복사 정리** — 본문을 마우스로 끌어 블록 복사할 때 묵상 노트 📄 배지가 클립보드 텍스트에
  섞여 들어가지 않습니다.
- **전방위 코드 모듈화(내부)** — 프론트엔드(`app.js`)와 백엔드(`api.py`), 가이드(`사용법.html`)를 기능별
  모듈로 분리해 안정성과 유지보수성을 높였습니다. 사용자 동작은 동일합니다.
- **스마트 검색 기반(개발)** — 형태소 분석 기반 다중 키워드 검색의 토대를 추가했습니다. (배포본은 안정성을
  위해 기존 검색 방식을 유지하며, 형태소 검색은 추후 정식 탑재 예정.)

---

## v1.0.3 — 원어 통합 검색 · 묵상 노트 · 데이터 정책 정리
- **저작권 가드 / 데이터 분리** — 기본 배포는 **개역한글(KRV)만** 동봉합니다. 다른 역본(ESV·개역개정 등)과
  원어 사전은 권리상의 이유로 빼고, 사용자가 **본인이 가진 모듈을 직접 추가**하는 외장형으로 바꿨습니다.
  (저장소·릴리스에서 저작권 데이터 제거. `사용법` 1번의 모듈 추가 안내 참고.)
- **원어 통합 검색바** — 검색창 하나로 ① 구절 이동(`창 1:1`), ② 키워드 검색(`사랑`),
  ③ 원어코드 역검색(`H7225` → 그 단어가 든 구절)을 처리하고, 책 이름 자동완성(`창`→`창세기`)을 제공합니다.
  원어 검색은 개역한글의 스트롱 태그 기반이라 사전 모듈 없이도 동작합니다.
- **묵상 노트** — 구절을 **우클릭 → 묵상 노트 쓰기**. 노트가 있는 구절에는 📄 배지가 붙고,
  마우스를 올리면 미리보기, 배지를 누르면 다시 편집할 수 있습니다(우클릭 메뉴에 구절 복사·원어 조회도).
- **분할 비교 스크롤 동기화** — 같은 장을 띄운 두 성경 카드를 나란히 두면 함께 스크롤됩니다.
- **첫 실행 패치노트 / 강제 업데이트** — 업데이트 후 첫 실행 시 변경 내역 팝업('다시 보지 않기' 지원),
  그리고 관리자가 권장 버전을 지정하면 구버전에 업데이트 안내 모달이 뜹니다.
- **비즈니스 가드(기반)** — 프리미엄/무료 분기와 익명·fail-open 실행 카운터의 토대를 추가(기본값은 전체 기능).
- 클립보드 모니터링 안정화, 잠금/활성 카드 표시 등 다수 개선.

---

## v1.0.2 — 카드 내비게이션 · 콤마 구절 · 활성 카드
- **카드별 독립 탐색 히스토리(◀ ▶)** — 각 성경 카드의 머리말에 ◀ ▶ 버튼이 생겼습니다.
  카드마다 방문한 구절(책·장·절)을 따로 기억해, 한 카드에서 뒤로/앞으로 가도 **다른 카드는
  영향을 받지 않습니다.** 뒤로/앞으로 갈 때는 그때 보고 있던 절이 화면 맨 위로 복원됩니다.
- **불연속 구절(콤마) 인식** — `요 1:1-2,4-6` 처럼 콤마로 끊어 적은 구절을 한 번에 인식해
  1·2·4·5·6절을 빠짐없이 불러옵니다.
- **한 줄 출력의 구획 구분(`//`)** — 출력 방식이 '한 줄'일 때, 콤마로 끊어진 구절 묶음 사이에
  ` // ` 구분 기호가 들어가 어디서 끊겼는지 한눈에 보입니다(연속된 절은 그대로 띄어쓰기).
- **활성 카드 강조** — 마지막으로 클릭/조작한 카드의 테두리에 보라색 네온 강조가 들어와
  지금 어떤 카드가 선택돼 있는지 분명해졌습니다. 키보드 ← / → 장 이동도 **이 활성 카드**를
  대상으로 동작합니다(이전에는 항상 첫 카드만 바뀌던 것을 수정).
- **잠금 카드 표시 정리** — 잠근 카드는 외곽 테두리 대신 **머리말 색조 변화 + 자물쇠 점등**으로
  표시해, 활성 카드의 네온 강조와 헷갈리지 않습니다.
- **성능** — 카드를 드래그·리사이즈하는 동안에는 저장을 멈춰 더 부드럽고, 스크롤이 멈춘
  뒤에만 위치를 기록하도록 다듬었습니다.

---

## v1.0.1 — 모듈형 카드 워크스페이스
- **자유 배치 카드 시스템** — 기존 고정 3분할(본문/원어/사전) 화면을, 원하는 위치에
  자유롭게 놓고 크기를 조절하는 **카드 워크스페이스**로 전환했습니다.
  - **자유 이동·리사이즈** — 카드 머리말을 끌어 이동, 가장자리·모서리(8방향)를 끌어 크기 조절.
  - **스냅(Attach)** — 다른 카드나 작업영역 가장자리에 가까워지면 살짝 떨어진 간격(거터)을
    두고 착 달라붙고, 정렬 가이드선이 표시됩니다.
  - **분할 가이드 스냅** — 작업영역의 2·3·4등분 지점(점선 가이드)에도 맞춰집니다.
  - **밀어내기(Push)** — 붙어 있는 카드 쪽으로 키우면 옆 카드가 크기를 유지한 채 밀려나며,
    경계에 닿으면 멈춥니다.
  - **겹침 허용** — 카드를 자유롭게 겹칠 수 있고, 클릭한 카드가 맨 앞으로 나옵니다.
- **카드 추가** — 상단의 `＋ 본문 / ＋ 원어 / ＋ 사전` 버튼으로 카드를 추가합니다(화면 중앙에 생성).
- **다역본 병렬 보기 복원** — 상단 역본 칩으로 역본을 관리하고(드래그로 순서 변경, 라이브
  애니메이션), 모든 본문 카드에 함께 반영됩니다.
- **조건부 수신(잠금)** — 본문 카드를 잠그면 클립보드 구절은 잠그지 않은 첫 카드가 받습니다.
  단, 잠긴 카드가 마침 그 책·장을 보고 있으면 그 카드가 우선 반응합니다.
- **본문 ↔ 원어 실시간 스크롤 동기화** — 본문을 스크롤하면 연결된 원어 카드가 최상단에
  온전히 보이는 절을 기준으로 따라갑니다.
- **사전 연결(link) 라우팅** — 원어 카드에서 단어를 클릭하면 그 카드와 연결된 사전 카드만 갱신됩니다.
- **레이아웃 저장** — 카드 배치가 저장되어 다음 실행 시 복원됩니다.
- 그 밖에 드롭다운이 카드에 가려지던 문제 등 다수 안정화.

---

## v1.0.0 — BibleClip 정식 출시 🎉
- **프로젝트 이전 & 정식 1.0.0** — 기존 `AutoBible` 저장소에서 **`tpwns432-maker/BibleClip`** 으로 이전하고, 정식 버전 **1.0.0** 으로 새 출발합니다. 앱 기능은 직전 개발본과 동일하며, 저장소·자동 업데이트·릴리스 주소가 모두 새 `BibleClip` 저장소로 바뀌었습니다.
- **원격 킬 스위치(remote kill switch)** — 관리자가 배포된 앱을 원격으로 정지할 수 있습니다. 앱은 시작 시 공개 매니페스트(`killswitch.json`)를 확인하고, `disabled`가 `true`이거나 `min_version`보다 낮은 버전이면 안내 메시지만 띄우고 실행을 차단합니다.
  - **되돌리기 가능** — 데이터는 건드리지 않으며, 매니페스트를 되돌리면 다음 실행 때 정상 복구됩니다.
  - **fail-open** — 인터넷이 끊겼거나 확인에 실패하면 평소대로 실행됩니다(네트워크 장애로 전체가 멈추는 사고 방지).
  - 이 확인 코드가 포함된 **v1.0.0부터** 적용됩니다(이전 AutoBible 시절 버전은 영향 없음).
- **문서** — 사용법에 성경 번역본(MyBible 모듈) 다운로드 안내(ph4.org) 추가.

---

> 아래는 이전 **AutoBible** 저장소 시절의 변경 이력입니다 (참고용 보존).

## v1.5.6
- **버그 수정** — 책/장(및 역본) 드롭다운 목록을 열고 **목록 안에서 스크롤하면 메뉴가 즉시 닫히던 문제** 수정. 특히 한 번 닫았다가 다시 열면 매번 재발하던 현상(메뉴를 닫을 때 스크롤 감지 리스너가 정리되지 않고 누적되어, 이전 메뉴의 낡은 리스너가 새 메뉴를 닫던 것이 원인).

## v1.5.5
- **검색 역본 선택** — 검색 화면의 입력창 옆에서 검색할 성경 역본을 직접 고를 수 있습니다(기본값은 현재 보고 있는 역본). 바꾸면 즉시 다시 검색합니다.
- **앱 설정 창(⚙)** — 좌측 레일 맨 아래 톱니 아이콘에서 앱 전반 설정을 엽니다(기존 "출력 설정"과 별개):
  - 클립보드 확인 간격(빠름 0.25초 / 기본 0.5초 / 느림 1초) — 실행 중에도 즉시 반영
  - 사전 기본 언어(한글/영어)
  - 검색 결과 클릭 시 본문으로 이동 토글
  - 시작 시 자동 업데이트 확인 on/off
  - 버전 정보, 데이터 폴더 열기, GitHub 열기, 설정 초기화
- **검색 결과 클릭 동작 선택** — 위 설정을 켜면 검색 결과를 클릭할 때 복사와 함께 해당 구절의 본문으로 이동합니다(끄면 기존처럼 복사만).

## v1.5.4
- **앱 엔진을 웹(pywebview) UI로 전환** — 리디자인 리포트의 High 단계. 기존
  CustomTkinter UI를 대체합니다. 기능은 동등하며 더 현대적인 화면으로 재구성:
  - 좌측 레일(성경 보기 · 출력 설정 · 검색 · 클립보드 로그) + 상단바 + 카드형 3분할(본문/원어/사전)
  - **클립보드 모니터링** — 복사한 구절을 자동 인식·변환·재복사, 캡처 시 본문 자동 이동·하이라이트 + 활동 로그 드로어
  - **다역본 병렬 보기** — 역본 칩 다중 선택 + 드래그로 순서 조정(라이브 애니메이션)
  - **출력 설정** — 표기/괄호/구분기호/다절 방식 등 + 출력 순서(↑↓, FLIP 애니메이션) + 실시간 미리보기
  - **검색** — `#키워드` 복사 시 자동 검색, 결과 클릭 복사
  - **원어 사전** — 단어 클릭(히브리어 48px 헤드워드+음역), 마우스 hover 미리보기, 우클릭 새 창, 한/영 전환
  - **본문 수동 복사**(절 클릭/드래그) · 본문↔원어 스크롤 동기화 · 키보드 ←/→ 장 이동
  - 글자 크기 A±, 다크 모드(달 아이콘 채움/비움), 마지막 위치·창 크기/위치 기억
  - 업데이트 확인 + 인앱 자동 설치(다운로드→교체→재시작)
- **번들 폰트** — Pretendard 포함. **Windows는 WebView2(Win11 기본 탑재), macOS는 내장 WKWebView** 사용(별도 런타임 불필요).
- 내부: UI 비의존 `Library` 코어로 로직 단일화(데스크톱/웹 공용), 헤드리스 테스트 추가.

## v1.5.3
- UI 현대화 마무리 — **출력 설정 탭**도 CustomTkinter로 통일 (남아 있던 옛 tk 스타일 제거)
  - 표기 설정(책 이름·장절 표기·괄호·표기 위치·범위/구분 기호·다절 출력 방식)을
    **세그먼트 버튼**으로, 체크 항목은 CTk 체크박스로 — 상단바·탭과 동일한 룩
  - 버전 선택/순서 영역을 카드로, 우측 설정 영역을 **CTk 스크롤 프레임**으로 재구성
    (이전의 전역 휠 스크롤 간섭 버그도 해소)
  - 라이트/다크 자동 전환 (기능·설정 동작은 그대로)
- 사전 팝업 z-order 수정 — 여러 사전창 중 하나를 닫을 때 **메인 창 뒤로 보낸 창이
  잘못 앞으로 튀어나오던 문제** 해결 (Windows의 실제 z-order를 직접 조회)

## v1.5.2
- UI 현대화 (CustomTkinter 도입 — 리디자인 Medium 단계)
  - 상단바·탭(세그먼트 컨트롤)·상태 배지를 모던 컴포넌트로
  - 본문 / 원어 / 사전 / 로그를 둥근 **카드** + CTk 스크롤바로 재구성
  - 내비게이션 컨트롤(책·장 드롭다운, 버튼, 입력칸, 사전 한/영 토글)을 CTk 라운드 컨트롤로
  - 책·장 드롭다운을 **휠 스크롤 가능한 커스텀 위젯**으로 (UI 폰트, 다시 누르면 닫힘)
  - 버전 칩 줄도 카드로 통일, 칩 드래그에 **부드러운 라이브 reorder 애니메이션**(좌우 대칭 기준)
  - 보라/인디고 팔레트, 라이트/다크 자동 전환
  - 책을 바꾸면 자동으로 1장으로 이동
  - 기능·데이터·단축키는 그대로 (설정 탭은 다음 버전에서 현대화 예정)

## v1.5.1
- 내부 구조 대규모 모듈화 (기능 100% 동일, 동작 변경 없음)
  - 단일 파일 `autobible.py`(3,764줄)를 `bibleclip/` 패키지로 분리
    (config·constants·theme·text_utils·update / data·core / UI 믹스인 10개)
  - 진입점을 `bibleclip_app.py`로 정리(`python -m bibleclip`도 지원), `autobible` 흔적 제거
  - 설정 파일명 `autobible_settings.json` → `bibleclip_settings.json`
    (기존 설정은 최초 실행 시 자동 승계 — 무손실, 옛 파일 보존으로 롤백 안전)
  - 저장소 정리: 잡파일 삭제(약 19.5MB), 문서→`docs/`, 빌드 스크립트→`packaging/`
  - GitHub 저장소명(AutoBible)·자동 업데이트 동작은 그대로

## v1.5.0
- 디자인 리프레시 (1단계: 현재 구조 유지, 기능 동일)
  - 보라/인디고 계열로 라이트·다크 팔레트 현대화, 단일 강조색 통일
  - 성경 본문에 세리프 글꼴 적용(책 느낌), UI·원어·사전은 기존 고딕 유지
  - 본문/패널 여백·줄 간격 정돈, 사전 Strong's 링크 색을 강조색에 맞춤

## v1.4.1
- 검색 정확도/편의 개선 (FTS5 없이 순수 파이썬, 읽기 전용 DB·macOS에서도 안전)
  - **띄어쓰기 완전 무시**: "태초에하나님이" = "태초에 하나님이" = "태초에  하나님이"
  - **퍼지 매칭**: 긴 문장의 오타·조사 변화도 trigram 겹침 랭킹으로 가장 가까운 구절을 찾음
  - 정제 본문·trigram을 버전별로 1회 캐싱 → 첫 검색 이후 즉시

## v1.4.0
- 키워드 검색 기능 추가
  - 상단 "검색" 입력란 또는 클립보드에 `#키워드`(예: `#태초에`)를 넣으면, 그 단어가 들어간 구절을 본문 패널에 `(창 1:1) 본문...` 형식으로 모두 표시
  - 검색 대상은 현재 보는 버전(기본 KRV)
  - 결과 구절을 클릭하면 출력 설정대로 복사
  - 클립보드로 `#키워드` 입력 시 첫 구절을 자동 복사

## v1.3.1
- 띄어쓰기 없는 구절 인식 버그 수정 (핵심) — "요15:4", "시23:1", "마28:19" 등이
  책 이름에 장 숫자 첫 자리를 먹어 인식 실패하던 문제 (책 이름 정규식 수정).
  띄어쓰기가 있는 "요 15:4"도 그대로 정상 동작.
- 새 설치 시 출력 버전이 비어 모니터링 결과가 안 나오던 문제 — 기본 출력 버전을 KRV로 채움

## v1.3.0
- 앱 이름을 **AutoBible → BibleClip** 으로 변경 (창 제목·실행 파일·릴리스 파일명)
  - 기존 사용자는 이번 버전을 한 번 수동 설치하면 이후 자동 업데이트가 정상 동작
  - macOS의 기존 앱 이름 충돌("AutoBible 2.app") 문제도 함께 해소
- macOS 한글 클립보드 인식 오류 수정 (핵심)
  - Finder에서 실행한 .app은 LANG 환경변수가 없어 `pbpaste`가 한글을 `?`로 깨뜨리던 문제 → `LANG`/`LC_ALL`을 UTF-8로 강제
  - `pbpaste`/`pbcopy`를 절대경로로 호출 + 번들의 DYLD_* 환경변수 정리
- macOS 앱이 "손상됨(damaged)"으로 매번 차단되던 문제 수정
  - 서명 후 복사를 `cp -R` → `ditto`로 바꿔 코드 서명 보존 (첫 실행 시 우클릭 → 열기 한 번이면 됨)
- macOS 자동 업데이트 추출 오류("AutoBible.app이 없습니다") 수정 — 페이로드 폴더를 정확히 탐색
- macOS 업데이터가 이름이 다른 번들(예: 기존 앱과 충돌 시 "AutoBible 2.app")도 정확히 교체·재실행하도록 수정
- macOS 앱 버전 정보(Info.plist)를 실제 버전으로 기록 (기존 0.0.0)

## v1.2.5
- macOS에서 클립보드 모니터링(메인 기능)이 동작하지 않던 문제 수정
  - macOS는 `pbpaste`/`pbcopy`로 클립보드를 읽고 쓰도록 변경 (Windows는 기존 방식 유지)
- 원어 단어 클릭 시 **같은 Strong's 코드 단어를 모두 하이라이트**
- **←/→ 키로 이전/다음 장 이동** (입력란에 포커스가 없을 때)
- 글꼴 크기(A- / A+, Ctrl+휠)를 **원어·사전 패널에도 적용**

## v1.2.4
- macOS 자동 업데이트의 SSL 인증서 오류(CERTIFICATE_VERIFY_FAILED) 수정
  - 다운로드에 certifi 인증서 사용 + 실패 시 무검증 폴백 (Windows는 영향 없음)
- 자동 업데이트 실패 시 "다운로드 페이지에서 직접 받기" 안내 알림 추가

## v1.2.3
- 스크롤바를 잘 보이게 개선 — 막대(thumb)에 대비 색을 적용해 현재 위치를 확인할 수 있도록 (모든 패널·팝업·설정 공통)
- macOS 앱이 "손상되어 열 수 없음(damaged)"으로 차단되던 문제 수정 — 번들에 데이터를 넣은 뒤 ad-hoc 코드 재서명

## v1.2.2
- macOS에서 성경/원어 데이터를 못 읽던 문제 해결
  - 데이터를 `.app` 번들 안에 포함시켜 앱 이동·App Translocation과 무관하게 항상 로드
  - 데이터 탐색 경로를 다중화(앱 옆 → 번들 내부)
- macOS용 `.dmg` 설치 파일 제공 (응용 프로그램으로 드래그 설치). 자동 업데이트는 기존대로 zip 사용

## v1.2.1
- 프로그램 전체 폰트를 OS 기본 고딕 하나로 통일 (Windows: 맑은 고딕, macOS: Apple SD Gothic Neo)
- 원어 호버 미리보기 창 크기 확대 (더 넓고 읽기 쉽게)
- 활동 로그의 구절 줄을 클릭하면 해당 구절로 이동 (기존 드롭다운 제거, 현재 세션 한정)
- 원어 단어를 **Ctrl+클릭 / 우클릭**(macOS: **⌘+클릭 / 두 손가락 클릭**)하면 독립 사전 창이 열림
  - 형태소 분석 + 사전 함께 표시, 계단식 배치, 마지막에 닫은 창 크기 기억, ESC로 닫기, 재시작 시 자동으로 다시 열지 않음

## v1.2.0
- 마지막으로 본 책/장을 기억해 재시작 시 복원
- 3분할(본문/원어/사전) 및 로그 패널의 분할 위치 기억
- 원어 단어 클릭 시 **형태소 분석** 표시 (원전분해 기반: 기본형·음역·품사·뜻)
- 원어 단어 **호버 미리보기** 툴팁
- macOS 자동 설치(인플레이스 업데이트) 지원
- macOS 앱 아이콘(.icns)을 CI에서 자동 생성

## v1.1.8
- 멀티 플랫폼 릴리스에서 실행 중인 OS에 맞는 zip을 자동 선택하도록 수정
  (Windows에서 Mac용 zip을 받아 업데이트가 실패하던 문제 해결)

## v1.1.7
- GitHub Actions 기반 Windows + macOS 자동 빌드/릴리스 도입 (태그 푸시 시 자동)
- macOS/Linux에서도 안전하게 실행되도록 플랫폼 분기 처리 (Windows 동작은 그대로)
- 플랫폼별 기본 폰트 적용, macOS .app 데이터 경로 보정

## v1.1.6
- 원어 데이터 폴더명을 `BethlehemWin` → `original_lang`으로 변경 (기존 이름도 인식)
- 릴리스 zip에 원어 데이터(개역한글S, 사전) 동봉 → 압축 해제 즉시 원어 기능 사용 가능

## v1.1.5
- 자동 업데이트 파일 교체 오류 수정
  - 한글 경로 대응(배치 파일 cp949 인코딩), robocopy 사용, 창 없는 실행, 즉시 종료 처리
- 릴리스 zip에 KRV 성경 포함

## v1.1.4
- 본문 ↔ 원어 패널 스크롤 연동 정확도 개선
  - 표시줄(wrap 포함) 기준 스크롤로 최상단 클립 현상 제거
  - "위가 잘리지 않은 첫 절"을 최상단 절로 판정

## v1.1.0 – v1.1.3 (개발 단계)
- 히브리어/헬라어 **원어 사전 패널** 도입 (Strong's 코드 기반)
- 성경 보기 탭에 3분할(본문 / 원어 / 사전) 통합 + 하단 가로 로그
- 원어 패널을 KRV(개역한글) 기준으로 표시하고, 단어 클릭 시 사전 조회
- 사전 한글/영어 토글
- 본문 ↔ 원어 스크롤 연동, 복사 시 해당 절을 최상단으로 정렬

## v1.0.2
- "업데이트 확인" 수동 버튼 추가
- SSL 검증 실패 시 폴백, 진단 로그(update_check.log) 기록

## v1.0.1
- 시작 시 기본 한국어 버전을 KRV로 변경
- 본문 드래그 선택 시 색상 대비 수정(선택 글자가 잘 보이게)

## v1.0.0 (최초 릴리스)
- 클립보드 모니터링 → 성경 구절 자동 변환/복사
- 여러 성경 버전 병렬 보기 + 드래그 & 드롭으로 순서 변경
- 출력 형식 설정(한국어/영어 공통 적용)
- 절 클릭/드래그로 복사
- 글꼴 크기 조절(A- / A+, Ctrl+휠)
- 다크 / 라이트 모드
- GitHub 기반 자동 업데이트(알림 배너 + 인플레이스 설치)
